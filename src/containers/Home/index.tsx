'use client';

type Props = {};

export default function HomePageContainer(props: Props) {
    return (
        <div className="mt-10">
            {/* <div className="">{currentUser.name}</div> */}
            Alo 1234
        </div>
    );
}
